[package]
name = "config"
version = "0.1.0"
edition = "2024"

[lib]

[dependencies]
log = { version = "0.4.28" }
config = { version = "0.15.14" }
serde = { version = "1.0.226", features = ["derive"] }

[lints.rust]
unsafe_code = "forbid"